# Lib rMLines sys

Check out the project on [Github](https://github.com/RedTTGMoss/librm_lines)!

This package holds the Emscripten build of lib rMLines.

It is published for both `x64` and `arm64` npm CPU targets on Linux, macOS, and Windows.

> Note that this package only includes the bindings.